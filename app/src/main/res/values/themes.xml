<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidSplashTutorial" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/teal_200</item>
        <!-- si la imagen tiene fondo se debe usar una imagen de 240x240dp y luego nuestra propia splash lo convertira en un circulo de 160 de diametro -->
        <!-- si la imagen NO tiene un background la tendremos que hacer de 288x288dp y luego el splash automaticamente hara un circunferencia con un diametro de 192dp -->
        <item name="android:windowSplashscreenContent">@drawable/sherk_splash</item>
        <!--duracion de la animacion-->
        <item name="android:windowSplashScreenAnimationDuration">300</item>
        <!-- Cuando se termine la animacion debe pasar al style que te estoy indicando aca -->
        <item name="postSplashScreenTheme">@style/Theme.AndroidSplashTutorial</item>
    </style>
    
</resources>